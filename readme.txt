There is an ant target for running the harness in the NetBeans debugger. To
use it, set the command line arguments that you want to pass to WsTest
in nbproject/project.properties. Then right-click the project name and
click "Debug Harness."
